#include <iostream>
using namespace std;

int mostSumNum(int* arr, int size) {
    int sum = 0;

    return sum;
}

int main() {
    int size;
    cin >> size;

    int arr[size];
    for(int i = 0; i < size; i++) {
        cin >> arr[i];
    }

    
}